# Google Service Account Credentials
# Get these from your Google Cloud Console Service Account

# The email address of your service account
# Example: your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# The private key from your service account JSON file
# IMPORTANT: Copy the entire private key including "-----BEGIN PRIVATE KEY-----" and "-----END PRIVATE KEY-----"
# Replace actual newlines with \n
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Instructions:
# 1. Create a Service Account in Google Cloud Console
# 2. Create and download a JSON key for the Service Account
# 3. Enable the Google Sheets API in your project
# 4. Share your Google Sheet with the Service Account email (read access)
# 5. Copy client_email and private_key from the JSON to this file
# 6. Rename this file to .env.local

# Firebase Storage Configuration
# Get these from Firebase Console -> Project Settings -> General -> Your apps -> SDK setup and configuration
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Firebase Storage Instructions:
# 1. Create a Firebase project at https://console.firebase.google.com/
# 2. Enable Storage in Firebase Console -> Build -> Storage
# 3. Set Storage rules to allow authenticated uploads
# 4. Copy the config values from Project Settings

# Anthropic API Key (for OCR with Claude Vision)
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key
